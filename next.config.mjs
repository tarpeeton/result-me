/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["ucarecdn.com", "shotambrat.github.io"], 
    unoptimized: true, 
  },
  reactStrictMode: false, 
};

export default nextConfig;
